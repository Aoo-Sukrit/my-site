# ไฟล์ตัวอย่าง มีแต่ชื่อตัวแปร ไม่ใส่ค่าจริง — ไฟล์นี้ขึ้น git ได้
# วิธีใช้: ก๊อปเป็น .env.local แล้วกรอกค่าจาก
# Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
